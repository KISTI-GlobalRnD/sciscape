# Research Experiments

This directory holds exploratory scripts and preserved experiment outputs.
These files are useful for research audit trails, but they are not package APIs
or public examples.

Use `examples/` for small runnable public demos and `workspace/` for new local
generated outputs.
