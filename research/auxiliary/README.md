# Research Auxiliary Workspaces

This directory holds historical or experimental workspaces that are not part of
the supported package surface.

- `cpm-dendro/`: archived CPM dendrogram Rust/PyO3 research crate.

Main production backends remain at the repository root under `rust/` and
`rust-text/`.
